【当前目标】
完成 V1.2 迭代的【阶段 A：算法基建与模块抽离】，引入高内聚、低耦合的纯函数算法层，并上线极简的 3 档评分模型。

【本轮计划】
1. 新建独立的算法模块 `algo_sm2.h` 与 `algo_sm2.cpp`。
2. 抽离原 `review.cpp` 里的“面条式”间隔与掌握度计算代码，封装成纯函数。
3. 改造 `review.cpp` 的业务流，注入新的 `algo_sm2` 模块。
4. 将原本不符合人类直觉的评分（0=不会 1=模糊 2=会），修改为直觉更强的（1=忘记 2=模糊 3=记牢）。

【实现 / 修改】
完成了以下核心重构：

**1. 新增 `src/algo_sm2.h / algo_sm2.cpp`**
- 严格遵循“高内聚低耦合”，**没有 `#include "globals.h"`**，算法文件根本不知道 `cards` 和 `wrongs` 的存在。
- 定义了数据契约结构体 `ReviewResult`。
- 实现了纯函数 `calculateNextReview`，直接完成：1=打回原形，2=原地踏步维持间隔，3=倍增间隔并大幅增加掌握度。

**2. 删减并重构 `src/review.cpp`**
- 砍掉了顶部原有的 `calcNextInterval` 和 `calcNextMastery`。
- 修改 `reviewOneCard` 和 `reviewOneWrong`：
  - 提示文案变更为 `请评价掌握程度 (1=忘记  2=模糊  3=记牢)：`。
  - 获取用户的分数后，调用 `calculateNextReview` 获取计算结果，然后再分别赋值给全局对象的字段。
- 修改了 `showReviewHistory`：让展示的日志能够向后兼容，输出 1/2/3 对应的新中文标签。

【验证方式】
后台执行命令：`g++ src/*.cpp -o project1.exe -std=c++17 -Wall`。
**结果：Exit code 0。模块分离非常干净，编译完美通过！**

【本轮更新日志】
**【版本号】**
- 当前版本：v1.2-PhaseA
- 对比版本：v1.1-Final

**【本轮目标】**
实现核心复习算法的高内聚剥离，降低用户复习打分的心智负担。

**【解决的问题】**
1. 解决了 `review.cpp` 中“业务调度流程”与“算法数学公式”紧密耦合的坏味道。
2. 解决了以前 0,1,2 评分经常导致输入错乱、心智负担较重的问题。

**【新增内容】**
- 新增 `algo_sm2` 模块（作为新算法引擎的第一次试验）。
- 引入纯结构体 `ReviewResult` 作为业务与算法间的数据传输契约。

**【修改内容】**
- `review.cpp` 的复习输入提示全部替换为 1-3 档机制。
- 移除了原有的老算法公式。

**【代码级变更】**
- `algo_sm2.cpp`：`calculateNextReview` 采用多分支结构处理评分，1 分强制重置间隔并惩罚 `mastery - 20`；2 分保持旧间隔但微量加分；3 分加 20 分并阶段性翻倍间隔。
- `review.cpp`：由原来的多次调用变成单次调用 `ReviewResult rr = calculateNextReview(...)` 批量更新状态。

**【影响范围】**
- 今日复习流程、复习日志查询展示全部发生变化。
- 原有的 txt 数据文件由于仅记录状态而不记录算法，**不受任何格式影响**。

**【验证结果】**
全量代码编译完美。你可以本地运行并开始一次【今日复习】，随意输入 1、2 或 3，观察终端输出的掌握度与间隔跳变结果，验证新模型是否运作正常。

**【已知问题 / 剩余问题】**
- 记忆衰减机制尚未就位，长期未看的数据目前仍然只会堆积而不会主动掉掌握度。

