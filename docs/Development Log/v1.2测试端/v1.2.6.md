【当前目标】  
完成 v1.2.6：新增核心业务流程回归测试，覆盖“错题转知识卡片后可见且数据一致”。

【本轮计划】  
1. 梳理注册、登录、卡片、错题、转换的控制台输入流程。  
2. 新增隔离式 E2E 测试脚本。  
3. 校验 `cards.txt` / `wrongs.txt` 的转换结果。  
4. 更新 README 与迭代方案。  
5. 回归 v1.2.4 / v1.2.5 测试。

【实现 / 修改】  
新增 [tests/test_e2e_flow.ps1](<E:\VS project\code_c\project1-v1.2\tests\test_e2e_flow.ps1:1>)：从空临时数据目录启动，自动完成注册、登录、新增卡片、新增错题、错题转卡片、查看全部卡片、退出，并检查 `linkedCardId` 指向真实有效卡片。

更新 [README.md](<E:\VS project\code_c\project1-v1.2\README.md:152>)：补充 v1.2.6 E2E 测试命令、项目结构和当前测试限制。

更新 [v1.2测试后续迭代方案.md](<E:\VS project\code_c\project1-v1.2\v1.2测试后续迭代方案.md:247>)：标记 v1.2.6 已完成，记录验证结果，并把下一轮建议切到 v1.2.7。

【验证方式】  
已实际运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\test_e2e_flow.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_algorithm_tests.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_cli_checks.ps1
```

三条命令均通过，退出码均为 `0`。`.test_tmp` 已自动清理。

【本轮更新日志】  
【版本号】  
- 当前版本：v1.2.6  
- 对比版本：v1.2.5

【本轮目标】  
为核心业务链路建立自动化回归测试，重点保护“错题转知识卡片后可见”这个历史风险点。

【解决的问题】  
- v1.2.5 只覆盖纯算法，无法发现菜单流程、存储写入、错题转换之间的集成问题。  
- 错题转卡片后 `linkedCardId` 是否有效，之前仍主要靠人工检查。  
- README 和迭代方案缺少 E2E 测试入口说明。

【新增内容】  
- 新增 `tests/test_e2e_flow.ps1`：业务闭环回归测试脚本。  
- 新增临时运行数据目录 `.test_tmp/e2e_flow/case/data`，脚本运行时创建，结束后清理。

【修改内容】  
- README 增加“业务流程回归测试”说明。  
- 迭代方案更新 v1.2.6 完成状态、当前测试成熟度和下一轮建议。

【代码级变更】  
- `test_e2e_flow.ps1` 新增安全目录检查、临时数据初始化、自动化输入序列、文件级断言。  
- 脚本解析 `users.txt`、`cards.txt`、`wrongs.txt`，验证用户、手动卡片、错题、转换卡片和 `linkedCardId`。  
- 脚本末尾执行 `--check-data`，确保业务流程生成的数据能通过一致性检查。

【影响范围】  
- 不改变业务源码行为。  
- 新增一条核心 E2E 自动化测试能力。  
- 回归测试会重新编译 `project1-v1.2.exe`。

【验证结果】  
- v1.2.6 E2E：通过。  
- v1.2.5 算法测试：通过，33 个断言通过。  
- v1.2.4 CLI 检查：通过。  
- 临时目录清理：通过。

【已知问题 / 剩余问题】  
- 目前 E2E 脚本仍依赖“切换运行目录”实现数据隔离。  
- 还没有统一的一键总测试入口。  
- 测试输入使用 ASCII，避免 Windows PowerShell 5.1 无 BOM 中文脚本解析问题；中文输入能力仍由 `run_utf8_automation.ps1` 单独覆盖。

【当前状态】  
v1.2.6 已完成。当前项目已具备 CLI 数据检查、算法单元测试、核心业务 E2E 三层测试能力；距离完整测试闭环还差原生数据目录隔离和一键总测试入口。