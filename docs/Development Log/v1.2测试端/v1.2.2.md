【当前目标】
补齐数据一致性检查工具，并解决中文用户名在 PowerShell 自动化输入中的编码问题。

【实现 / 修改】
已完成本轮改动：

- [maintenance.cpp](<E:\VS project\code_c\project1-v1.2\src\maintenance.cpp:216>)
  - 新增“数据一致性检查工具”。
  - 检查错题 `linkedCardId` 是否指向真实存在、属于当前用户、且未删除的卡片。
  - 检查卡片 / 错题的掌握度、难度、复习间隔、日期字段。
  - 对可明确修复的问题提供确认修复：失效关联重置为 `-1`，字段范围自动归正。

- [maintenance.cpp](<E:\VS project\code_c\project1-v1.2\src\maintenance.cpp:431>)
  - “数据维护”菜单新增：
    - `6. 数据一致性检查`

- [tools/run_utf8_automation.ps1](<E:\VS project\code_c\project1-v1.2\tools\run_utf8_automation.ps1:1>)
  - 新增 UTF-8 自动化运行脚本。
  - 显式设置 `InputEncoding / OutputEncoding / $OutputEncoding` 为 UTF-8。
  - 用 UTF-8 输入文件喂给程序，解决 PowerShell 管道中文输入乱码问题。

- [tools/sample_login_zh_input.txt](<E:\VS project\code_c\project1-v1.2\tools\sample_login_zh_input.txt:1>)
  - 新增中文登录 smoke test 输入样例。

- [utils.cpp](<E:\VS project\code_c\project1-v1.2\src\utils.cpp:211>)、[main.cpp](<E:\VS project\code_c\project1-v1.2\src\main.cpp:121>)
  - 处理输入流 EOF，避免自动化输入耗尽后程序无限循环刷菜单。

- [README.md](<E:\VS project\code_c\project1-v1.2\README.md:79>)
  - 补充自动化中文输入说明和 `ExecutionPolicy Bypass` 运行方式。
  - 补充数据一致性检查说明。

【验证方式】
已验证：

```powershell
g++ -std=c++17 -o project1-v1.2.exe src/*.cpp
```

编译通过。

已运行中文用户名自动化登录：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_utf8_automation.ps1
```

结果：`张钰清` 登录成功，正常退出。

已运行“数据一致性检查”自动化流程，结果：

```text
发现问题：0 项
可自动修复：0 项
```

【本轮更新日志】
- 当前版本：v1.2.2
- 对比版本：v1.2.1

本轮目标：补齐数据一致性检查，并解决中文自动化输入不稳定问题。

解决的问题：
- 错题可能残留失效 `linkedCardId`，现在可在数据维护中扫描并重置。
- PowerShell 默认管道编码可能导致中文用户名登录失败，现提供 UTF-8 runner 脚本。
- 自动化输入耗尽后程序可能无限循环，现主菜单和关键子菜单可在 EOF 时返回或退出。

新增内容：
- 数据一致性检查菜单项。
- UTF-8 自动化运行脚本。
- 中文登录输入样例。

修改内容：
- README 增加数据检查与中文自动化输入说明。
- 程序输入处理增加 EOF 保护。

影响范围：
- 数据维护模块。
- 自动化测试流程。
- 控制台菜单输入稳定性。

已知问题 / 剩余问题：
- PowerShell 执行策略可能禁止直接运行 `.ps1`，需要用 README 中的 `-ExecutionPolicy Bypass` 方式。
- 当前一致性检查是交互式工具，不是命令行 `--check-data` 模式。

