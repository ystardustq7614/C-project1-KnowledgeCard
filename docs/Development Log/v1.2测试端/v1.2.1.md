虽然这次更新并不是直接引入测试端的，但作为直接原因，还是记录于此。

【当前目标】
修复“错题转知识卡片后，生成卡片在知识卡片管理中找不到”的问题。

【实现 / 修改】
已完成修复，涉及：

- [wrong.cpp](<E:\VS project\code_c\project1-v1.2\src\wrong.cpp:675>)
  - 转换前检查 `linkedCardId` 指向的卡片是否真实存在且当前用户可见。
  - 如果旧关联卡片缺失，允许重新生成并修复关联。
  - 将生成卡片背面的“错因分析”从换行改为单行，避免破坏 `cards.txt` 一行一条记录的格式。
  - 转换成功后刷新知识卡片展示缓存。

- [card.cpp](<E:\VS project\code_c\project1-v1.2\src\card.cpp:15>) / [card.h](<E:\VS project\code_c\project1-v1.2\src\card.h:14>)
  - 新增 `resetCardDisplayCache()`，避免卡片管理继续使用旧列表。

- [storage.cpp](<E:\VS project\code_c\project1-v1.2\src\storage.cpp:51>)
  - 新增 `sanitizeField()`，保存用户、卡片、错题文本字段时清理 `|`、`\r`、`\n`，防止文本字段破坏数据文件结构。

- [cards.txt](<E:\VS project\code_c\project1-v1.2\data\cards.txt:7>)
  - 补回当前已损坏数据中缺失的 `cardId=7` 知识卡片，使现有错题 `linkedCardId=7` 能对应到真实卡片。

【验证方式】
已执行：

```powershell
g++ -std=c++17 -o project1-v1.2.exe src/*.cpp
```

编译通过。

还检查了数据一致性：

- `cards.txt` 中已存在 `cardId=7`
- `wrongs.txt` 中错题 `linkedCardId=7`
- `cards.txt` 每行均为 17 个字段
- `wrongs.txt` 当前错题为 18 个字段

说明当前数据文件格式已恢复一致。

【本轮更新日志】
- 当前版本：v1.2.1
- 对比版本：v1.2

本轮目标：修复错题转知识卡片后卡片不可见的问题。

解决的问题：转换卡片时生成的背面内容包含换行，可能破坏 `cards.txt` 的单行记录格式；同时旧数据出现“错题已关联 cardId，但卡片文件中没有对应卡片”的不一致状态。

新增内容：新增卡片展示缓存清理函数；新增保存字段清理函数。

修改内容：错题转卡片逻辑现在会校验关联卡片是否真实存在；生成卡片的错因分析改为单行保存；保存数据时统一清理危险分隔字符。

代码级变更：`wrong.cpp` 增加关联卡片可见性检查和脏关联修复逻辑；`card.cpp/card.h` 增加缓存重置入口；`storage.cpp` 增加字段清理并接入保存流程；`cards.txt` 补齐缺失的转换卡片记录。

影响范围：错题转知识卡片、知识卡片列表展示、文本文件持久化。

验证结果：编译通过；数据字段数量和 `linkedCardId=7` 关联关系已检查通过。未完成真实控制台端到端登录验证，因为 PowerShell 管道传中文用户名时编码不匹配。

已知问题 / 剩余问题：控制台自动化测试中文输入仍受 PowerShell 管道编码影响，手动运行程序不受这个问题影响。

