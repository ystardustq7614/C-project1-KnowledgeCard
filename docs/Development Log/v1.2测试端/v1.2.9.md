【当前目标】  
完成 v1.2.9：补齐测试文档与失败排查指南。

【本轮计划】  
1. 新增 `docs/testing.md`。  
2. README 增加测试文档入口。  
3. 迭代方案标记 v1.2.9 已完成。  
4. 运行一键总测试验证当前状态。

【实现 / 修改】  
已新增 [docs/testing.md](<E:\VS project\code_c\project1-v1.2\docs\testing.md:1>)，覆盖：

- 推荐运行方式：`tools/run_all_tests.ps1`
- 测试分层：总入口、CLI、算法、E2E
- 每个脚本的用途和命令
- `--data-dir` / `PROJECT1_DATA_DIR` 数据隔离机制
- 主程序和测试脚本退出码
- 编译失败、脚本执行策略、CLI、算法、E2E 常见失败排查
- 当前测试边界和维护原则

已更新 [README.md](<E:\VS project\code_c\project1-v1.2\README.md:198>)：增加 `docs/testing.md` 入口、项目结构、已知限制和后续建议。

已更新 [v1.2测试后续迭代方案.md](<E:\VS project\code_c\project1-v1.2\v1.2测试后续迭代方案.md:469>)：标记 v1.2.9 已完成，并把下一轮建议切到 v1.3.0 测试闭环版本。

【验证方式】  
已实际运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_all_tests.ps1
```

结果：退出码 `0`。`.test_tmp` 已清理。

【本轮更新日志】  
【版本号】  
- 当前版本：v1.2.9  
- 对比版本：v1.2.8

【本轮目标】  
让测试体系不仅能用一条命令运行，还能被新用户理解和维护。

【解决的问题】  
- v1.2.8 已有总入口，但缺少系统化测试说明。  
- 测试失败时，用户需要从脚本和输出里自行推断排查路径。  
- `--data-dir`、`PROJECT1_DATA_DIR`、退出码、`-KeepTemp` 等测试机制没有集中说明。

【新增内容】  
- 新增 `docs/testing.md`：测试说明与失败排查指南。

【修改内容】  
- README 增加测试文档入口。  
- README 项目结构补充 `docs/testing.md`。  
- 迭代方案更新 v1.2.9 完成状态和 v1.3.0 下一步。

【代码级变更】  
- 本轮没有修改 C++ 源码或测试脚本逻辑。  
- 新增文档明确了现有脚本职责、数据流和失败排查路径。

【影响范围】  
- 不影响程序运行行为。  
- 不影响测试执行逻辑。  
- 提升测试体系可读性和后续维护性。

【验证结果】  
- 一键总测试：通过。  
- CLI 检查、算法测试、E2E 流程均由总入口覆盖通过。  
- 临时目录清理：通过。

【已知问题 / 剩余问题】  
- E2E 仍只覆盖一条核心业务主链路。  
- 中文自动化输入仍由独立 UTF-8 脚本维护。  
- `algo_decay` 日期差仍是简化实现。

【当前状态】  
v1.2.9 已完成。项目现在具备测试执行入口、分层测试、数据隔离、失败排查文档，已经接近 v1.3.0 测试闭环版本。