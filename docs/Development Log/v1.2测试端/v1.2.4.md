【当前目标】
完成 v1.2.4：测试数据隔离与 CLI 检查脚本。

【本轮计划】
已完成：
- 新增隔离测试 fixture。
- 新增 `run_cli_checks.ps1`。
- 覆盖 `0 / 1 / 2` 退出码场景。
- 确认测试不污染真实 `data/`。
- 更新 README 与测试迭代方案。

【实现 / 修改】
新增文件：

- [tools/run_cli_checks.ps1](<E:\VS project\code_c\project1-v1.2\tools\run_cli_checks.ps1:1>)
  - 编译主程序。
  - 创建 `.test_tmp/cli_checks` 临时运行目录。
  - 复制 fixture 到临时 `data/`。
  - 在临时目录中运行 `project1-v1.2.exe`。
  - 验证干净数据、坏关联、非法字段、参数错误、不存在用户等场景。
  - 测试结束后安全清理 `.test_tmp`。

- `tests/fixtures/clean_data/`
- `tests/fixtures/broken_link_data/`
- `tests/fixtures/invalid_field_data/`

修改文件：

- [README.md](<E:\VS project\code_c\project1-v1.2\README.md:118>)
  - 新增 CLI 检查脚本说明。

- [v1.2测试后续迭代方案.md](<E:\VS project\code_c\project1-v1.2\v1.2测试后续迭代方案.md:97>)
  - 标记 v1.2.4 已完成。
  - 下一轮建议更新为 v1.2.5 算法单元测试。

【验证方式】
已运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_cli_checks.ps1
```

验证通过，覆盖：

- clean data：`exit=0`
- user scoped check：`exit=0`
- invalid parameter：`exit=2`
- missing user：`exit=2`
- broken link detection：`exit=1`
- broken link auto fix：`exit=0`
- broken link post-fix：`exit=0`
- invalid field detection：`exit=1`
- invalid field auto fix：`exit=0`
- invalid field post-fix：`exit=0`

额外确认：
- `.test_tmp` 已清理。
- 真实 `data/cards.txt` 仍为 17 字段格式。
- 真实 `data/wrongs.txt` 仍为 18 字段格式，`linkedCardId=7` 未被测试污染。

【本轮更新日志】
- 当前版本：v1.2.4
- 对比版本：v1.2.3-test-plan

本轮目标：让 CLI 数据检查具备隔离测试能力，并形成可一键运行的测试脚本。

解决的问题：
- 测试不再依赖真实 `data/`。
- `--check-data` 的成功、失败、修复、参数错误场景都被脚本覆盖。
- 失效 `linkedCardId` 修复结果现在有自动验证。

新增内容：
- `tools/run_cli_checks.ps1`
- `tests/fixtures/clean_data/`
- `tests/fixtures/broken_link_data/`
- `tests/fixtures/invalid_field_data/`

修改内容：
- README 增加 v1.2.4 CLI 检查脚本说明。
- 测试后续迭代方案更新 v1.2.4 状态和下一轮方向。

代码级变更：
- 本轮未改 C++ 业务源码。
- 新增 PowerShell 测试脚本，负责构造隔离运行目录、执行 CLI、校验退出码和修复结果。
- 新增 fixture 数据文件，用于稳定复现干净数据、坏关联和非法字段场景。

影响范围：
- 自动化测试流程。
- 测试数据组织结构。
- 项目测试文档。

已知问题 / 剩余问题：
- 目前还没有算法单元测试。
- 业务流程回归测试还没覆盖“新增错题 -> 转卡片 -> 查看卡片”的完整交互链路。
