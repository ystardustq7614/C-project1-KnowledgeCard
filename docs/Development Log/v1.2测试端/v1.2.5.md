【当前目标】  
完成 v1.2.5：为算法模块建立可独立运行的单元测试闭环。

【本轮计划】  
1. 新增算法断言测试。  
2. 新增一键运行算法测试脚本。  
3. 更新 README 与迭代方案。  
4. 实际运行 v1.2.5 测试，并回归 v1.2.4 CLI 检查。

【实现 / 修改】  
已新增 [tests/test_algorithms.cpp](<E:\VS project\code_c\project1-v1.2\tests\test_algorithms.cpp:1>)：覆盖 `algo_sm2`、`algo_decay`、`algo_recommend`，共 33 个断言。

已新增 [tools/run_algorithm_tests.ps1](<E:\VS project\code_c\project1-v1.2\tools\run_algorithm_tests.ps1:1>)：编译并运行算法测试，临时产物放在 `.test_tmp/algorithm_tests`，结束后自动清理。

已更新 [README.md](<E:\VS project\code_c\project1-v1.2\README.md:135>)：补充算法测试命令、项目结构，并修正“没有自动化测试脚本”的过时描述。

已更新 [v1.2测试后续迭代方案.md](<E:\VS project\code_c\project1-v1.2\v1.2测试后续迭代方案.md:167>)：将 v1.2.5 标记为已完成，记录验证结果，并把下一轮建议切换到 v1.2.6。

【验证方式】  
已实际运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_algorithm_tests.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_cli_checks.ps1
```

结果：两条命令均退出码 `0`。算法测试 33 个断言全部通过；v1.2.4 CLI 检查也保持通过。`.test_tmp` 已清理。

【本轮更新日志】  
【版本号】  
- 当前版本：v1.2.5  
- 对比版本：v1.2.4

【本轮目标】  
让算法模块脱离控制台交互和真实数据文件，能够独立编译、独立运行、自动断言验证。

【解决的问题】  
- v1.2.4 只有 CLI 数据一致性检查，算法调整仍缺少回归保护。  
- `algo_sm2`、`algo_decay`、`algo_recommend` 之前没有自动化断言测试。  
- README 中“没有自动化测试脚本”的描述已不符合当前状态。

【新增内容】  
- 新增 `tests/test_algorithms.cpp`：算法单元测试入口。  
- 新增 `tools/run_algorithm_tests.ps1`：算法测试编译与运行脚本。

【修改内容】  
- README 增加算法测试说明和项目结构中的 `tests/tools`。  
- 迭代方案将 v1.2.5 标记为已完成，并更新下一轮方向为 v1.2.6。

【代码级变更】  
- `test_algorithms.cpp` 新增 `expectEqual`、`expectTrue`、`expectFalse`、`expectInRange`。  
- 新增 SM-2 测试：忘记、模糊、记牢、掌握度边界、间隔递增。  
- 新增衰减测试：宽限期、3 天触发、掌握度下限 30、间隔下限 1。  
- 新增推荐测试：空输入、Top N、章节聚合、平均掌握度排序、同分题量优先。  
- `run_algorithm_tests.ps1` 只链接 `tests/test_algorithms.cpp` 和 `src/algo_*.cpp`，不链接 `main.cpp`，不读写 `data/`。

【影响范围】  
- 不改变业务运行逻辑。  
- 新增算法测试能力。  
- README 与迭代方案状态更新。  
- `project1-v1.2.exe` 在回归 CLI 检查时被重新编译。

【验证结果】  
- v1.2.5 算法测试：通过。  
- v1.2.4 CLI 回归检查：通过。  
- 临时测试目录清理：通过。

【已知问题 / 剩余问题】  
- 业务流程回归测试尚未建立。  
- 真实业务闭环仍需要 v1.2.6 覆盖“错题转卡片后可见”。  
- `algo_decay` 日期差仍是简化实现，不适合严格日历计算。

【当前状态】  
v1.2.5 已完成并通过验证。当前项目测试能力已覆盖“CLI 数据一致性检查 + 纯算法单元测试”；完整测试闭环尚未完成，项目闭环报告建议等 v1.3.0 汇总时输出。