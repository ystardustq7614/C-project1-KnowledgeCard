【当前目标】  
完成 v1.2.8：新增一键测试总入口，统一验证编译、CLI、算法和 E2E。

【本轮计划】  
1. 新增 `tools/run_all_tests.ps1`。  
2. 串联现有测试脚本。  
3. 更新 README 与迭代方案。  
4. 实际运行总入口验证。

【实现 / 修改】  
已新增 [tools/run_all_tests.ps1](<E:\VS project\code_c\project1-v1.2\tools\run_all_tests.ps1:1>)。

脚本执行顺序：

```text
清理 .test_tmp
编译主程序
运行 CLI 数据一致性检查
运行算法单元测试
运行业务流程 E2E
确认临时目录清理
```

已更新 [README.md](<E:\VS project\code_c\project1-v1.2\README.md:198>)：新增“一键测试总入口”说明，并更新项目结构。

已更新 [v1.2测试后续迭代方案.md](<E:\VS project\code_c\project1-v1.2\v1.2测试后续迭代方案.md:403>)：将 v1.2.8 标记为已完成，记录验证结果，并把下一轮建议切到 v1.2.9。

【验证方式】  
已实际运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\run_all_tests.ps1
```

结果：退出码 `0`。`.test_tmp` 最终已清理。

【本轮更新日志】  
【版本号】  
- 当前版本：v1.2.8  
- 对比版本：v1.2.7

【本轮目标】  
把 v1.2.4 到 v1.2.7 建立的测试能力串成一条统一命令。

【解决的问题】  
- 之前需要分别运行 CLI、算法、E2E 三个脚本。  
- 缺少一个统一的失败入口和阶段输出。  
- 测试闭环还不能用一条命令验证。

【新增内容】  
- 新增 `tools/run_all_tests.ps1`：总测试入口。

【修改内容】  
- README 增加总测试命令。  
- 迭代方案更新 v1.2.8 完成状态和下一步建议。

【代码级变更】  
- `run_all_tests.ps1` 新增 `Invoke-TestStep()`，统一输出 `START / PASS` 阶段。  
- 先编译主程序，再调用 `run_cli_checks.ps1 -SkipBuild` 和 `test_e2e_flow.ps1 -SkipBuild`，避免重复编译主程序。  
- 使用哈希表命名参数 splatting 调用子脚本，避免 PowerShell 把 `-ExePath` 当作位置参数。  
- 任一阶段失败会抛错并返回 `1`；全部通过返回 `0`。

【影响范围】  
- 不改变业务源码逻辑。  
- 新增一条测试运行入口。  
- 测试流程更容易被重复执行和验收。

【验证结果】  
`run_all_tests.ps1` 已覆盖并通过：

- 主程序编译：通过。  
- CLI 数据一致性检查：通过。  
- 算法单元测试：通过。  
- E2E 业务流程：通过。  
- 总入口退出码：`0`。  
- 临时目录清理：通过。

【已知问题 / 剩余问题】  
- 还缺独立测试文档与失败排查指南。  
- E2E 主链路仍使用 ASCII 输入以保证 PowerShell 5.1 稳定。  
- `algo_decay` 日期差仍是简化实现。


【当前状态】  
v1.2.8 已完成。当前项目已经可以用一条命令完成编译、CLI、算法和 E2E 验证，测试闭环的运行入口已经建立。